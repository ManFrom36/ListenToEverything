<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!-- Set the theme of the Activity that directly follows your splash screen. This is required. -->
        <item name="postSplashScreenTheme">@style/ListenToEverything</item>
    </style>

    <style name="ListenToEverything" parent="Theme.AppCompat.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="colorAccent">@color/lte_blue</item>
    </style>

    <style name="LTERatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">#ECEAD9</item>
        <item name="colorControlActivated">@color/lte_blue</item>
    </style>

</resources>